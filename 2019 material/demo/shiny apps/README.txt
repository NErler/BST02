# In order to run the shiny apps you need to perform the following steps:

# 1) Download the folder 'shiny apps'
# 2) Install the following packages:
#       install.packages("shiny")
#       install.packages("JM")
#       install.packages("DT")
#       install.packages("lattice")
#       install.packages("effects")

# 3) Open the.R files in Rstudio
# 4) You can either run the code in the normal way (line by line / select all) or you can press 'Run App'


