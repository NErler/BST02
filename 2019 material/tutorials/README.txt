# In order to run the interactive tutorials you need to perform the following steps:

# 1) Download the folder 'tutorials' with all the files including the subfolders
# 2) Install the following packages: 
#       install.packages("learnr")
#       install.packages("JM")
#       install.packages("memisc")
#       install.packages("foreign")

# 3) Open the .Rmd files (Markdown files) in R studio
# 4) Press 'Run Document'

# After running the interactive tutorials a HTML file is also created which is a 
# simplified version of the interactive tutorial